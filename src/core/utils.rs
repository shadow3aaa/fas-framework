use super::{Status, Jank};
use crate::misc;

pub fn check() -> Jank {
    
}

impl Status {
    pub fn new -> Status {
        Status {
            fresh_rate : 0,
            top_app : String::new(),
        }
    }
    pub fn update(&mut self) {
    
    }
}